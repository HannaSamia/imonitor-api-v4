# Implementation Plan: Phase 3.10 — Env Centralization Tech Debt

**Track ID:** phase3.10-env-centralization_20260313
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-13
**Status:** [x] Complete

## Overview

Three phases: register the missing env vars in validation, inject ConfigService into
the four affected services and replace all `process.env` reads, then verify.
No test changes required — specs mock DB calls, not env vars.

## Phase 1: Env Validation

Add missing variables to the Joi schema so they are validated and defaulted at startup.

### Tasks

- [x] Task 1.1: Add `DB_CORE_NAME: Joi.string().default('iMonitorV3_1')` to `src/config/env.validation.ts`

### Verification

- [ ] `npm run build` passes with no errors

## Phase 2: Service Fixes

Inject `ConfigService` into each affected service and replace every `process.env`
read with `this.configService.get<string>('VAR_NAME')`.

### Tasks

- [x] Task 2.1: Fix `customer-care-sdp-trace.service.ts` — inject `ConfigService`, replace `DB_CORE_NAME` and `DB_DATA_NAME`
- [x] Task 2.2: Fix `customer-care-history.service.ts` — inject `ConfigService`, replace `DB_CORE_NAME` and `DB_DATA_NAME` (6 occurrences)
- [x] Task 2.3: Fix `customer-care-air-trace.service.ts` — inject `ConfigService`, replace `DB_CORE_NAME` and `DB_DATA_NAME`
- [x] Task 2.4: Fix `tarrif-log.service.ts` — inject `ConfigService`, replace `DB_DATA_NAME`

### Verification

- [x] `npm run build` passes
- [x] `npm run lint` passes (no unused-vars warnings introduced)

## Phase 3: Final Verification

### Tasks

- [x] Task 3.1: Run `npm test` — confirm all 1397 tests still pass
- [x] Task 3.2: Grep `process\.env` in `src/` — confirm no remaining hits in main-thread services
- [~] Task 3.3: Commit, push branch, merge to main, tag `v0.3.10-env-centralization`

## Final Verification

- [x] All acceptance criteria met
- [x] `npm run build && npm run lint && npm test` all pass
- [x] No `process.env` in main-thread services outside exempt files
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
